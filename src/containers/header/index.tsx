import { Box } from "@mui/material"
import { Navbar } from "../../components/navbar"

export const Header: React.FC<{}> = () => {
    return (
        <Box>
            <Navbar />
        </Box>
    )
}